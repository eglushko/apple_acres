import classes from './Products.module.css';

const Product = (props) => {

    return (
        <section className={classes.products}>
            <ul>
            </ul>
        </section>
    );
};

export default Product;